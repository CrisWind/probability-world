/* Probability World: read-only achievement book and transient unlock toast. */
(function attachAchievementUI(global) {
  'use strict';
  var store = global.__gameStore;
  var bus = global.GameEventBus;
  var data = global.AchievementData;
  var locks = global.GameInputLocks;
  if (!store || !bus || !data) { console.error('[AchievementUI] required runtime is missing.'); return; }

  var categoryLabels = { milestone: '主线里程碑', exploration: '探索发现', relationship: '人物关系', judgement: '数理判断', investigation: '调查与证据', restraint: '风险与克制', hidden: '隐藏行为' };
  var categoryOrder = ['milestone', 'exploration', 'relationship', 'judgement', 'investigation', 'restraint', 'hidden'];
  var groupLabels = { 'group.clear_ledger': '多路径选择', 'group.harbor_observer': '长期累计' };
  var toastQueue = [], toasted = new Set(), toastTimer = null, flushTimer = null, toastBusy = false, bookOpen = false, bookLock = 'achievement-book';

  var style = document.createElement('style');
  style.textContent = '#achievement-book-button{position:absolute;left:58px;top:96px;z-index:29;width:44px;height:44px;padding:0;border:1px solid #3E2A1B;background:#6B4A2F;color:#E8D7B0;font:18px NotoPixelCN,sans-serif;box-shadow:2px 2px 0 #14100C;cursor:pointer;pointer-events:auto}#achievement-book-button:hover{background:#7D5A3A;border-color:#E8C87A}#achievement-book-button:active{transform:translate(2px,2px)}#achievement-book-overlay{position:fixed;inset:0;z-index:100100;display:none;align-items:center;justify-content:center;padding:24px;box-sizing:border-box;background:rgba(8,12,18,.76);pointer-events:none}#achievement-book-overlay.is-open{display:flex;pointer-events:auto}#achievement-book-panel{position:relative;width:min(860px,calc(100vw - 32px));max-height:min(760px,calc(100vh - 48px));overflow:hidden;box-sizing:border-box;padding:26px;background:#E8D7B0;color:#211B17;border:4px solid #3E2A1B;box-shadow:0 0 0 2px #C89B3C,5px 5px 0 #14100C}#achievement-book-header{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;padding-bottom:14px;border-bottom:2px solid #B18A45}#achievement-book-title{margin:0;color:#6D3028;font-size:26px}#achievement-book-subtitle{margin:5px 0 0;color:#6B4A2F;font-size:13px}#achievement-book-close{min-width:48px;min-height:48px;border:2px solid #3E2A1B;background:#8C3A2E;color:#E8D7B0;font:20px NotoPixelCN,sans-serif;cursor:pointer}#achievement-book-content{max-height:calc(100vh - 190px);overflow-y:auto;padding:18px 4px 4px}.achievement-category{margin:0 0 22px}.achievement-category-title{display:flex;align-items:center;gap:10px;margin:0 0 10px;padding-bottom:6px;color:#17324A;font-size:17px;border-bottom:1px solid #B18A45}.achievement-category-count{color:#8D6B32;font-size:12px}.achievement-entry{margin:0 0 10px;padding:14px 16px;background:#F2E5C8;border:2px solid #B18A45;box-shadow:2px 2px 0 rgba(33,22,15,.28)}.achievement-entry.is-unlocked{background:#FFF4D7;border-color:#C89B3C}.achievement-entry-head{display:flex;align-items:center;justify-content:space-between;gap:12px}.achievement-entry-title{margin:0;color:#3E2A1B;font-size:17px}.achievement-entry-status{flex:none;color:#8C3A2E;font-size:12px}.achievement-entry.is-unlocked .achievement-entry-status{color:#6B4A2F}.achievement-entry-description{margin:7px 0 0;line-height:1.65;font-size:13px}.achievement-entry-meta{margin:7px 0 0;color:#8D6B32;font-size:11px}.achievement-entry-tags{margin-top:8px;color:#6B4A2F;font-size:11px}.achievement-empty{padding:18px;text-align:center;color:#6B4A2F}@media(max-width:600px){#achievement-book-button{left:58px;top:96px}#achievement-book-overlay{padding:16px}#achievement-book-panel{width:calc(100vw - 32px);max-height:calc(100vh - 32px);padding:18px}#achievement-book-title{font-size:21px}#achievement-book-content{max-height:calc(100vh - 150px);padding-top:14px}.achievement-entry{padding:13px}.achievement-entry-title{font-size:15px}.achievement-entry-head{align-items:flex-start;flex-direction:column;gap:4px}}#achievement-unlock-toast{position:fixed;right:20px;top:20px;z-index:100200;width:min(320px,calc(100vw - 40px));box-sizing:border-box;padding:13px 16px;background:#2A1E14;color:#E8D7B0;border:2px solid #C89B3C;box-shadow:3px 3px 0 #14100C;pointer-events:none;opacity:0;transform:translateY(-8px);transition:opacity .16s ease,transform .16s ease}#achievement-unlock-toast.is-visible{opacity:1;transform:translateY(0)}.achievement-toast-kicker{color:#E8C87A;font-size:11px}.achievement-toast-title{margin-top:4px;color:#FFF0BE;font-size:16px}.achievement-toast-copy{margin-top:4px;color:#C9B283;font-size:12px;line-height:1.4}';
  document.head.appendChild(style);

  var entry = document.createElement('button'); entry.id = 'achievement-book-button'; entry.type = 'button'; entry.setAttribute('aria-label', '打开成就簿'); entry.title = '成就簿'; entry.textContent = '◆';
  var overlay = document.createElement('div'); overlay.id = 'achievement-book-overlay'; overlay.setAttribute('aria-hidden', 'true');
  var panel = document.createElement('section'); panel.id = 'achievement-book-panel'; panel.setAttribute('role', 'dialog'); panel.setAttribute('aria-modal', 'true'); panel.setAttribute('aria-labelledby', 'achievement-book-title');
  panel.innerHTML = '<div id="achievement-book-header"><div><h2 id="achievement-book-title">成就簿</h2><p id="achievement-book-subtitle">记录那些由选择留下的航海见闻。</p></div><button id="achievement-book-close" type="button" aria-label="关闭成就簿">×</button></div><div id="achievement-book-content"></div>';
  overlay.appendChild(panel); document.body.appendChild(entry); document.body.appendChild(overlay);
  var toast = document.createElement('div'); toast.id = 'achievement-unlock-toast'; toast.setAttribute('aria-live', 'polite'); toast.setAttribute('aria-atomic', 'true'); document.body.appendChild(toast);

  function stateAchievements() { var value = store.get('achievements', {}); return value && typeof value === 'object' ? value : {}; }
  function isUnlocked(instance) { return !!(instance && instance.unlockedAt !== null && instance.unlockedAt !== undefined); }
  function dateText(value) { if (!value) return ''; var date = new Date(value); return Number.isNaN(date.getTime()) ? '' : date.toLocaleDateString('zh-CN'); }
  function displayFor(definition, instance) {
    var unlocked = isUnlocked(instance), visibility = definition.visibility || 'visible';
    if (visibility === 'hidden' && !unlocked) return { title: '???', description: '尚未揭晓', status: '尚未揭晓', tags: '' };
    if (visibility === 'named_hidden' && !unlocked) return { title: definition.title, description: '继续探索以揭开条件。', status: '未解锁', tags: '' };
    return { title: definition.title, description: definition.description || '', status: unlocked ? '已解锁' : '未解锁', tags: groupLabels[definition.groupId] || '', date: unlocked ? dateText(instance.unlockedAt) : '' };
  }
  function renderBook() {
    var content = document.getElementById('achievement-book-content'); if (!content) return;
    var instances = stateAchievements().instances || {}, definitions = data.DEFINITIONS || {}, groups = {};
    Object.keys(definitions).forEach(function (id) { var definition = definitions[id]; var category = definition.category || 'hidden'; (groups[category] || (groups[category] = [])).push({ definition: definition, instance: instances[id] || null }); });
    var html = '';
    categoryOrder.forEach(function (category) { if (!groups[category] || !groups[category].length) return; var unlockedCount = groups[category].filter(function (x) { return isUnlocked(x.instance); }).length; html += '<section class="achievement-category"><h3 class="achievement-category-title">' + (categoryLabels[category] || category) + '<span class="achievement-category-count">' + unlockedCount + '/' + groups[category].length + '</span></h3>'; groups[category].forEach(function (item) { var view = displayFor(item.definition, item.instance), unlocked = isUnlocked(item.instance); html += '<article class="achievement-entry ' + (unlocked ? 'is-unlocked' : '') + '"><div class="achievement-entry-head"><h4 class="achievement-entry-title">' + view.title + '</h4><span class="achievement-entry-status">' + view.status + '</span></div><p class="achievement-entry-description">' + view.description + '</p>' + (view.tags ? '<div class="achievement-entry-tags">' + view.tags + '</div>' : '') + (view.date ? '<div class="achievement-entry-meta">记录于 ' + view.date + '</div>' : '') + '</article>'; }); html += '</section>'; });
    content.innerHTML = html || '<div class="achievement-empty">暂时还没有登记成就。</div>';
  }
  function openBook() { if (bookOpen) return; bookOpen = true; renderBook(); locks && locks.acquire && locks.acquire(bookLock); overlay.classList.add('is-open'); overlay.setAttribute('aria-hidden', 'false'); document.getElementById('achievement-book-close').focus(); }
  function closeBook() { if (!bookOpen) return; bookOpen = false; overlay.classList.remove('is-open'); overlay.setAttribute('aria-hidden', 'true'); locks && locks.release && locks.release(bookLock); flushToast(); entry.focus(); }
  function blockedForToast() { if (bookOpen) return true; var scene = global.pixelHarborScene; if (scene && (scene.dialogVisible || scene.panelOpen || scene.introActive || scene.transitioning)) return true; return !!document.querySelector('#dialogue-ui.show,#inventory-overlay.is-open,#notice-overlay.is-open,#captain-overlay.show,.human-card-overlay.show,.human-card-open'); }
  function scheduleToastFlush() { if (flushTimer) return; flushTimer = setTimeout(function () { flushTimer = null; flushToast(); }, 220); }
  function showNextToast() { if (toastBusy || !toastQueue.length || blockedForToast()) { if (toastQueue.length && !toastBusy) scheduleToastFlush(); return; } var item = toastQueue.shift(), definition = item.definition; toastBusy = true; toast.innerHTML = '<div class="achievement-toast-kicker">成就已记录</div><div class="achievement-toast-title">' + definition.title + '</div><div class="achievement-toast-copy">' + (definition.description || '') + '</div>'; toast.classList.add('is-visible'); toastTimer = setTimeout(function () { toast.classList.remove('is-visible'); toastTimer = setTimeout(function () { toastBusy = false; toastTimer = null; showNextToast(); }, 160); }, 2300); }
  function flushToast() { if (!toastBusy) showNextToast(); }
  function onUnlock(event) { var payload = event && event.payload, id = payload && payload.achievementId, definition = id && data.getDefinition ? data.getDefinition(id) : (id && (data.DEFINITIONS || {})[id]); if (!definition || toasted.has(id)) return; toasted.add(id); toastQueue.push({ id: id, definition: definition }); scheduleToastFlush(); }
  entry.addEventListener('click', openBook); panel.querySelector('#achievement-book-close').addEventListener('click', closeBook); overlay.addEventListener('click', function (event) { if (event.target === overlay) closeBook(); }); document.addEventListener('keydown', function (event) { if (bookOpen && event.key === 'Escape') { event.preventDefault(); closeBook(); } });
  bus.on('achievement_unlocked', onUnlock); store.subscribe(function () { if (bookOpen) renderBook(); });
  global.AchievementUI = Object.freeze({ open: openBook, close: closeBook, render: renderBook, flushToast: flushToast });
})(window);
